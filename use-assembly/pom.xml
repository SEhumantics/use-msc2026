<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <parent>
        <groupId>org.tzi.use</groupId>
        <artifactId>use</artifactId>
        <version>7.5.0</version>
    </parent>
    <modelVersion>4.0.0</modelVersion>

    <packaging>pom</packaging>
    <artifactId>use-assembly</artifactId>

    <dependencies>
        <dependency>
            <groupId>org.tzi.use</groupId>
            <artifactId>use-gui</artifactId>
            <version>7.5.0</version>
        </dependency>
        <!-- Declared so Maven's reactor builds kk-modelvalidator before this assembly step, exactly
             like the use-gui dependency above: assembly.xml pulls the built jar from
             msc-modelvalidators/kk-modelvalidator/target, same pattern as use-gui.jar from
             use-gui/target. Dependency resolution is by GAV, not path, so this reference is
             unaffected by kk-modelvalidator's move under msc-modelvalidators/. -->
        <dependency>
            <groupId>org.tzi.use</groupId>
            <artifactId>kk-modelvalidator</artifactId>
            <version>7.5.0</version>
        </dependency>
        <dependency>
            <groupId>org.tzi.use</groupId>
            <artifactId>unc-modelvalidator</artifactId>
            <version>7.5.0</version>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <artifactId>maven-assembly-plugin</artifactId>
                <version>3.3.0</version>
                <configuration>
                    <descriptors>
                        <descriptor>src/assembly/assembly.xml</descriptor>
                    </descriptors>
                    <finalName>use-${project.version}</finalName>
                </configuration>
                <executions>
                    <execution>
                        <id>make-assembly</id>
                        <phase>package</phase>
                        <goals>
                            <goal>single</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
