# Historical USE-Uncertainty corpus entries that the port does not reproduce.
#
# Each line is `<corpus file>:<line>  # <cause>` and is checked by
# UncertaintyCompilerCorpusTest: an unlisted divergence fails as a regression,
# and an entry that starts agreeing must be deleted from this file. The list is
# the honest record of what is left.
#
# All five remaining entries have been traced to causes outside the uncertainty
# semantics, and the behaviour each of them was asserting is covered directly by
# UncertaintyQueryEvaluationTest.

# `.equals(...)` on a collection receiver is expanded to the collect shorthand
# by the compiler, so it evaluates per element and can never produce the single
# Boolean these entries expect. That expansion is identical in the historical
# and the current compiler, so these expectations were not reachable in the
# historical build either. The collection identities they state are asserted
# with `=` in UncertaintyQueryEvaluationTest.
UCollectionOperations.in:146  # `.equals' on a collection expands to collect shorthand
UCollectionOperations.in:149  # `.equals' on a collection expands to collect shorthand
UCollectionOperations.in:153  # `.equals' on a collection expands to collect shorthand
UCollectionOperations.in:168  # `.equals' on a collection expands to collect shorthand
UCollectionOperations.in:172  # `.equals' on a collection expands to collect shorthand
