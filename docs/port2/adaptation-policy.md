# Adaptation policy — normative for S3–S9

**Status:** governing document. Assembled from the six area readers in
`docs/port2/adaptation/01-types.md` … `06-collections.md`, all of which were read in full.
Where this document and an area document disagree, **this document wins**; where this document is
silent, the area document is the detail record.

**Scope of authority.** §1 is the rule. §2 is the work list. §3 is the waiver ledger. §4 is the
pre-registration of every deliberate deviation from the fork. §5 re-scores the open decisions.
§6 collects everything that is not yet established.

**Provenance note (2026-08-21).** Three of the six area readers this document cites for detail —
`adaptation/04-grammar.md`, `adaptation/06-collections.md`, and `spec-parts/13-grammar.md` — were
consolidated during a documentation cleanup and no longer exist as separate files. Every decision
those area readers argued for is already recorded in this document's own tables (§2, §4); the
deleted files' full original content remains in git history.

**Evidence tags used throughout:** `MEASURED` (code was executed and its output pasted in the area
document), `READ_FROM_SOURCE` (read at a named file:line), `INFERRED` (reasoned, not executed).
Every `INFERRED` row must be probed before its owning stage starts — §2.3 lists them.

---

## 1. The policy

> **Uncertainty meaning comes from the fork. Everything else comes from USE 7.5.0.
> Where the two collide, keep the uncertainty behaviour but express it the 7.5.0 way.**

This was decided by the user. It is not open for debate; a stage author's job is to apply it.

### 1.1 The worked example

| expression | fork (MEASURED, 2021 jars) | plain USE 7.5.0 (MEASURED, `use-core/target/classes`) | **the port must answer** |
|---|---|---|---|
| `Set{UReal(2,0.5), 1, 2.5}` | `Set(UReal)` | compile error `probe:1:4: Undefined operation 'UReal'.` | **`Set(UReal)`** |
| `Set{UReal(2,0.5), 1, 2.5}->sum()` | `UReal` / `UReal(5.5, 0.5)` | compile error | **`UReal`** |
| `Set{1, 2.5}` *(control)* | `Set(Real)` | `Set(Real)` | `Set(Real)` — both agree |
| `1/0` | `Undefined : OclVoid` | `null : OclVoid` | **`null : OclVoid`** |

Reproduced end-to-end by three independent readers (01 §4.1, 04 §3.1, 06 §1.1/§1.2).

### 1.2 How to apply the rule to a new case — four questions, in order

1. **Is the behaviour under dispute uncertainty meaning?** If a U-type, a U-value, an uncertainty
   operation or an uncertainty-lifted result is what makes the answer differ, the answer comes from
   the fork. `Set(UReal)` is uncertainty meaning. `Set(Real)` is not.
2. **Is it a defect?** If the fork's answer violates a contract it does not intend
   (`equals` reflexivity, `Comparable` antisymmetry, `equals`/`hashCode` consistency, a declared
   type that its own value does not conform to, an unguarded array index), **B7 applies: fix it and
   pre-register the fix in §4**. B7 is a *policy*, already decided; it is not a per-row licence to
   improvise.
3. **Is it merely older?** If the fork's version is 2015 upstream and 7.5.0 has moved on
   (`sortedBy`'s `OrderedSet` branch, `import java.util.*`, `visitObjOp`→`visitInstanceOp`,
   `Op_number_pow`/`Op_number_sqrt`, `qualifiedName()`, `modelQualifiedType`), **7.5.0 wins,
   unconditionally.** Taking the fork's file wholesale is how ten years of upstream drift gets
   silently deleted. **File-level copying from the fork is forbidden for every file marked "edit".**
4. **Does 7.5.0 already have a mechanism for it?** If so, use 7.5.0's mechanism rather than the
   fork's. `TypeFactory.buildInTypesMap` instead of grammar keywords; a semantic predicate on
   `IDENT` instead of an implicit token; two `return false;` roots inherited by six leaves instead
   of six edits; the named-operation path instead of a bespoke `identicalExpression` rule.

### 1.3 Three consequences the user has already accepted

* **B5** — the lattice falsifies 10 of the 12 assertions in upstream's untouched
  `TypeTest#testSupertype`. **One** written rule-3 waiver covers it (§3).
* **B6** — the port prints `null`; the 79 corpus entries expecting `Undefined` are normalised at
  comparison time by rule **N1**, never by reverting the port.
* **B7** — the 33 behaviour-changing ledger defects are FIXED and documented, not reproduced.
* **B2** — `SBoolean` is ported in full (39 operations), independently corroborated by a live
  registry dump: 39 anonymous `SBoolean` enum constants (03 §1.2).

---

## 2. The adaptation table

**75 collisions** as recorded by the six readers — **62 MEASURED · 12 READ_FROM_SOURCE · 1 INFERRED** —
presented below as **72 rows**, because three collisions were found independently by two readers each
and are merged into a single row (`V01 / P-01`, `V15 / K-10`, `V18 / P-04`). Row ids keep the
originating area's numbering so an area document can always be found from a row:
`T-` types (15) · `V` values (18) · `O-` operations (10) · `G-` grammar (5) · `P-` printing (9) ·
`K-` collections (18).

Stage column: the stage mapping below is **assigned by this document** from the record's known
anchors (B5 lands in S3; B7 fixes are "open work for S4–S7"; the corpus criterion is S8). It is not
an independently established stage plan and a stage author may find the boundaries drawn elsewhere;
the *area* attribution is what is load-bearing.

| stage | area | source document |
|---|---|---|
| **S3** | types, lattice, `TypeFactory` | `adaptation/01-types.md` |
| **S4** | values, `Value`/`CollectionValue`/`MathUtil` | `adaptation/02-values.md` |
| **S5** | operation registries, overload resolution | `adaptation/03-operations.md` |
| **S6** | grammar, parser, concrete syntax | `adaptation/04-grammar.md` |
| **S7** | collections, `ExpQuery`, visitors | `adaptation/06-collections.md` |
| **S8** | printing, corpus acceptance | `adaptation/05-printing-corpus.md` |
| **S9** | integration gate | — |

Sorted highest-impact first. `⚑` marks a row that needs probing or a decision **before** its stage
starts (INFERRED shape, or the two readers disagreed).

### 2.1 Tier 1 — rows that decide whether the port is the right port at all

| id | where | fork | 7.5.0 | **the PORT** | stage | conf |
|---|---|---|---|---|---|---|
| **T-08** ⚑ | `UniqueLeastCommonSupertypeDeterminator.calculateFor` :58-70 + `BasicType.hashCode` :57-59 | greedy `else if (t.conformsTo(result))` over a `HashSet`; `{Integer, UnlimitedNatural}` is mutually conformant and has no fixpoint. Same binary, two driver programs: `Set{*, 1}` = `Set(UnlimitedNatural)` 10/10 and `Set(Integer)` 10/10 | **identical defect.** Forcing identity-hash order in the same 7.5.0 binary flips the answer 3/3 each way | **give `calculateFor` a deterministic tie-break** (TreeSet by `toString()`, or require `!result.conformsTo(t)` before replacing) and record the deviation. A nondeterministic oracle cell makes Study A's agreement metric unmeasurable | S3 | MEASURED |
| **T-15** | `UnlimitedNaturalType` :61-63 / :69-76 (unedited by the fork) | `UnlimitedNatural.conformsTo(UInteger\|UReal)` = true, but `allSupertypes` not widened and `isKindOfUInteger/UReal` = false ⇒ pairwise `LCS(UnlimitedNatural, UInteger)` = `OclAny` while `ULCS({…})` = `UInteger` | same *shape* pre-exists: `Integer.conformsTo(UnlimitedNatural)` true, `UnlimitedNatural ∉ Integer.allSupertypes()` | **B11 stands open.** Standing recommendation is reproduce + pin `LCS(UnlimitedNatural, UInteger) == OclAny`. Two new facts: the divergence is observable at **expression** level, and the two LCS routines disagree — so "reproduce" means reproducing a self-inconsistent *pair* | S3 | MEASURED |
| **O-01** | `StandardOperationsNumber` :866-867 (`matches`), :876 (`eval` cast), registered :32 | `sqrt` bucket = `Op_ureal_sqrt`, `Op_uInteger_sqrt` only. `UReal(4,2).sqrt()`→`UReal`; `4.sqrt()`→compile error | `Op_number_sqrt` added 2024-06-27, **widened** 2024-06-29 to `isKindOfNumber(EXCLUDE_VOID)→mkInteger`. It **accepts** a ported `URealType` and, registered first, wins | **tighten `matches` with `&& !(params[0] instanceof UncertainType)`.** Then `UReal(4,2).sqrt()`→`UReal` **and** `4.sqrt()` keeps 7.5.0's answer. Do NOT move the registries earlier (22 measured regressions, O-07). Do NOT "fix" `mkInteger()`→`mkReal()` | S5 | MEASURED |
| **O-07** | `OpGeneric.registerOperations`; `ExpStdOp` :54 `ArrayListMultimap`, :90-95 / :121-127 first-match | uncertainty registries sit between `StandardOperationsBoolean` and `StandardOperationsCollection` (`:93-97`) | identical prefix `:82-90` and suffix `:93-97` with nothing between | **register the five uncertainty registries AFTER `StandardOperationsBoolean`, at the fork's slot.** Exhaustive 74 970-cell sweep: uncertainty-**first** changes 22 cells of ordinary OCL (String relationals and Boolean logic become `UBoolean`) — forbidden by the policy's second sentence; uncertainty-**last** changes 0. Separately, `ArithOperation.matches` must keep its branch order `isTypeOfInteger → isArgIntegerOrReal → LCS.isTypeOfUInteger → isKindOfNumber/mkUReal` or `1 + 1` becomes `UReal` | S5 | MEASURED |
| **K-08** | B9. `ExpQuery` :136, :206, :208 casts `(BooleanValue) queryVal`, reached from :161 | takes **both**: `assertBooleanQuery()`→`assertKindOfUBoolean()` **and** value-level `evalExists0`/`evalForAll0` folding through `ExpStdOp.create("or"/"and", …)`. `Set{1,2,UReal(2,5)}->forAll(e\|e>=1)` = `UBoolean(true, 0.579)`, does not throw | swapping the guard alone admits a `UBooleanValue` into the cast ⇒ `ClassCastException` at eval. Spec claim CONFIRMED from source | **TAKE BOTH** (B9 option 1). Reasons: uncertain `exists`/`forAll` is uncertainty meaning; `ExpQueryUncertaintyTest` :79-88 / :93-102 are unportable otherwise; `uSelect`/`uSelectC` already require the guard and the 5-arg ctor. Accepted cost: loses short-circuiting — mitigated by DEP-27 | S7 | READ_FROM_SOURCE + MEASURED |
| **G-01** ⚑ | `OCLBase.gpart` :132 (`DOT op='equals' LPAREN … RPAREN`) vs `t098.use:11`, `t133_import_date.use:29`, `t133_import_datetime.use:12` | `equals` is an implicit ANTLR literal ⇒ reserved in **all six** generated lexers (6 `match("…")` sites each). Only reachable form is `X.equals(Y)`. `t098.use` dies at `line 11:1 no viable alternative at input 'equals'` | `equals` is an ordinary `IDENT`; the failure is *semantic*, `Undefined operation named 'equals'`. `t098.use` compiles past line 11 to its expected pre-existing error | **delete `identicalExpression`; leave `expression`/`expressionOnly` byte-identical to 7.5.0; register the fork's `Op_identical` in the opmap.** MEASURED with **zero grammar edit**: `(1).equals(1)`→`Boolean true`; `Set{1}->equals(Set{1})`→`true`; `let equals : Integer = 1 in equals` works; `t098.use` and `t133_import_date.use` byte-identical to baseline; user-defined `equals` is **not** shadowed. Also fixes fork defects D1/D2 for free | S6 | MEASURED |
| **V01 / P-01** | `UndefinedValue.toString(StringBuilder)` — fork `:46` vs 7.5.0 `:43` | `sb.append("Undefined")` — the **sole** non-lexer producer of the token in the whole fork tree | `sb.append("null")` since upstream `72ab8fd7` (2019-06-27). The two files differ in exactly one behavioural line | **print `null`.** Normalise the 79 corpus rows at comparison time with rule **N1**, a whole-string map anchored on `"Undefined : OclVoid"` with a `contains` guard and a pinned firing count of exactly 79. Never a substring rewrite | S4/S8 | MEASURED |
| **V15 / K-10** ⚑⚠ | asymmetric cross-type `equals` × `HashSet` (`SetValue` :49) | `Set{1, UReal(1,0)}` → `Set{1}` (size 1, the UReal is **silently dropped**); `Set{UReal(1,0), 1}` → size 2. Root cause: `UReal(1,0).equals(1)`=true but `1.equals(UReal(1,0))`=false, `compareTo` 0 vs −36, identical hash `1072693248`. Also inconsistent with the language's own `=`, since `Set{UReal(1,0)} = Set{1}` is `false` | no cross-type numeric `equals`, so no asymmetry can arise | **DECISION NEEDED — the two readers disagree.** 02 §7-2 says it is *not* on the 33-row B7 list so B7 does not authorise a fix, and the only symmetric fix edits upstream `IntegerValue.equals`/`RealValue.equals`, exceeding the recorded minimal edits. 06 §1.6 calls it "squarely a B7 latent defect, FIXED not reproduced" and supplies the acceptance criterion without asserting the answer. Must be decided **before or with** DEP-03, because F-10 makes the same asymmetry appear for `UInteger` | S4 | MEASURED |
| **V18 / P-04** ⚑ | `.in` corpus vs `UBooleanValue.java:192-199` (jar bytecode offset 32 = `iconst_3`) | corpus expects 10-decimal confidences (`UBoolean(true, 0.5792596878)`); both the fork **source** and the fork **jar** round to **3**. Executed: MATCH=2, MISMATCH=8, every "got" exactly the 3-dp rounding of the "expected" | n/a | **DECISION NEEDED (R1).** Recommended **(a)**: follow the fork *code*, emit 3 decimals, pre-register the 8 rows as known non-agreements, consistent with B6's precedent of normalising the corpus not the port. Alternative **(b)**: emit 10 decimals and diverge from the fork's own `toString`. Either way this changes Study A's denominator. **The record's S8 criterion "1427 entries, 1427 passes" is unachievable by any correct port** | S8 | MEASURED |

### 2.2 Tier 2 — rows that change code the port must write

| id | where | fork | 7.5.0 | **the PORT** | stage | conf |
|---|---|---|---|---|---|---|
| **T-02** | `TypeImpl.java:313`, `MClassifierImpl.java:355` | ten new `Type` predicates given `return false;` at exactly two roots; `MClassifierImpl` had 2 subclasses | same two roots (`grep "public boolean isTypeOfOclAny()"` → `TypeImpl:313`, `MClassifierImpl:355`, `OclAnyType:33`; `use-gui` none) but **six** `MClassifierImpl` subclasses: `MClassImpl`, `MAssociationImpl`, `MAssociationClassImpl`, `MSignalImpl`, `MDataTypeImpl`, `EnumType` | put the ten `return false;` no-ops on `TypeImpl` and `MClassifierImpl` **only**; all six leaves inherit. `MDataTypeImpl`/`MSignalImpl` need no code of their own | S3 | MEASURED |
| **T-14** | `EnumType.java:39` | `EnumType extends MClassifierImpl` | identical | if the ten no-ops go only on `TypeImpl`, `EnumType` does not compile. Getting it right is why `Enum.allSupertypes() = [E, OclAny]` is one of only **two** surviving `testSupertype` assertions | S3 | MEASURED |
| **T-04** | fork `IntegerType.java:71` vs 7.5.0 `:59` — **byte-identical bodies** | `conformsTo` is plain virtual dispatch. `Integer ≤ UInteger` and `Integer ≤ UReal` are installed from the **supertype** side (`URealType.isKindOfNumber()→true`), so the *unmodified* `IntegerType.conformsTo` accepts them. Other edges come from the subtype side (`BooleanType.conformsTo` naming `other.isTypeOfUBoolean()`) | same dispatch shape, byte-identical `IntegerType.conformsTo` | replicate **both** idioms and put a javadoc note on `IntegerType.conformsTo` saying it is deliberately unchanged. A reviewer diffing that method sees nothing and will otherwise "restore symmetry" and silently widen the lattice | S3 | MEASURED |
| **T-12** | `TypeImpl.conformsTo` — fork `:76-78`, 7.5.0 `:78-81` | `return this.conformsTo(other);` — unbounded self-recursion, character-for-character the same in both trees. Fires only because every concrete type overrides it | identical defect | **every one of the five new concrete types MUST override `conformsTo`** or it `StackOverflowError`s. The two abstract tags (`UncertainType`, `UncertainBooleanType`) correctly do not. Add a unit test calling `conformsTo` on each of the five so the trap is caught at build time | S3 | READ_FROM_SOURCE |
| **T-10** | `ASTSimpleType.java:47` + `TypeFactory.buildInTypesMap` | `ASTSimpleType.gen` consults `mkSimpleType(name)` **before** enums, before `getClassifier(name)`, before the variable table. With `class UReal` declared: `UReal.allInstances()`→lexer error, `Person.allInstances()`→`Set(Person)`. The fork made the names grammar tokens too | map holds only `Integer, UnlimitedNatural, String, Boolean, Real, OclAny, OclVoid`; `class UReal` is a legal, usable model class | register **exactly five** `buildInTypesMap` entries and nothing more. The grammar half is G-04 and the two must agree on the same five names. No upstream test resource uses any of the five, so nothing upstream breaks — **but a user model can, and that is an accepted consequence to document** | S3 | MEASURED |
| **V02** | `Value.java` :36-190 | adds `isUInteger()/isUReal()/isUBoolean()/isSBoolean()`, each `public boolean` returning `false` | lacks those four; **every other member is byte-identical** — `diff -u` shows only those four hunks plus `$Id:` and a `@version` tag | add exactly those four non-abstract `false` predicates. **Zero signature adaptation anywhere else in the value layer.** No existing 7.5.0 value class is touched (incl. `DataTypeValueValue`, `InstanceValue`, which post-date the fork). Do **not** add `isUString()` — the fork did not and `UStringValue` discriminates by `instanceof` | S4 | MEASURED |
| **V03** | `URealValue:42`, `UIntegerValue:46`, `UBooleanValue:192`, `UStringValue:67`, `SBooleanValue:124` | **three** rounding regimes in one family: UReal/UInteger **10 dp**, UBoolean/SBoolean **3 dp**, UString **none** | no opinion | reproduce all three **verbatim**, including `UString`'s hard-coded `UString('` prefix instead of `type()`. Unifying them is a silent divergence from uncertainty meaning | S4 | MEASURED |
| **V17** | collection literal evaluation | static type is `Set(UReal)` but the value holds raw `IntegerValue`/`RealValue` elements: `Set{1,2.5,UReal(2.0, 0.5)}`; `->sum()` = `UReal(5.5, 0.5)` | compile error | keep. Elements are **NOT lifted** — the lattice works at the type level only. Every uncertain value class must therefore keep accepting `IntegerValue`/`RealValue` operands in `equals`, `compareTo`, `uEquals` and every arithmetic wrapper | S4 | MEASURED |
| **O-03** | 21 upstream op classes the fork rewrote in place (20 concrete + abstract `ArithOperation`), across `StandardOperationsNumber`, `…Any`, `…Collection` | produce `UReal`/`UInteger`/`UBoolean`/`SBoolean` results | originals produce `Real`/`Integer`/`Boolean` and cast to `RealValue`/`IntegerValue` | **three-way merge per member:** take the FORK's `matches` and `eval`; take 7.5.0's `name`, `kind`, `isInfixOrPrefix`, `checkWarningUnrelatedTypes` (measured identical in all 21 anyway). Exceptions: `Op_number_unaryplus` :365 and `Op_number_unaryminus` :327 already return `params[0]`, so their `matches` is correct as-is; only `unaryminus` needs the fork's `eval`. **Taking the fork's `StandardOperationsNumber.java` wholesale deletes `pow` and `sqrt` from OCL and silently breaks `t001.in:161-171`** | S5 | MEASURED |
| **O-02** | `StandardOperationsNumber` :819-822, :829-838, :815 | no `pow` at all (bucket size 0). The fork spells it `power`: `UReal(4,2).power(2)`→`UReal` | `4.pow(2)`→`Real`; `matches({UReal,Integer})`→`Real`; `eval` casts to `IntegerValue`/`RealValue` | same `!(instanceof UncertainType)` guard on **both** parameters. Port must give: `UReal(4,2).pow(2)`→clean compile error (as the fork does), `4.pow(2)`→`Real`, `UReal(4,2).power(2)`→`UReal`. **Do NOT alias `pow`→`power`** — that invents uncertainty meaning the fork does not have | S5 | MEASURED |
| **O-06** | `StandardOperationsUString` :19 and :21 | `Op_uString_uConcat` registered **twice** under `+`; its `matches` dereferences `params[1]` **before** the `params.length == 2` guard. Because `Op_number_unaryplus` (arity 1) shares the `+` bucket, `+ 'a'`, `+ true`, `+ Set{1}`, `+ UBoolean(true,1)`, `+ SBoolean(…)` all **THREW** `ArrayIndexOutOfBoundsException: Index 1 out of bounds for length 1` | clean diagnostics: `Undefined operation 'String.+()'.` etc., no exception | **B7 fix, and the fix restores 7.5.0's behaviour exactly.** Register once; hoist the length guard. Post-fix `+ 'a'` must produce 7.5.0's clean `Undefined operation 'String.+()'.` | S5 | MEASURED |
| **K-04** | `Op_collection_includes/_excludes/_includesAll/_excludesAll`, `matches` + `eval` | result type lifts `Boolean`→`UBoolean` when the LCS is an `UncertainType` (or the argument is an `UncertainValue`); `eval` dispatches on `resultType` to `coll.uIncludes/uExcludes/uIncludesAll/uExcludesAll` | all four unconditionally return `mkBoolean()` / `BooleanValue.get(…)` | take the fork's shape verbatim: 8 hunks, +51/−15. This is the core uncertainty meaning of the collection layer | S7 | READ_FROM_SOURCE |
| **K-06** | `CollectionValue.java` | 5 new concrete methods: `uIncludes`, `uIncludesAll`, `uExcludes`, `uExcludesAll`, `uCountC` | none exist | single hunk **+99 / −0**. **No existing method changes**; `SetValue`/`BagValue`/`SequenceValue`/`OrderedSetValue` need **no edit at all**. Reject the fork's expansion of `import java.util.*` into eight explicit imports — 7.5.0's form is the modern one. *The entire uncertain collection semantics is 5 added methods and 0 modified ones* | S7 | MEASURED |
| **K-03** | `Op_collection_sum.matches`/`.eval` (fork :596-599, :612-641) | `->sum()` over `Set(UReal)`→`UReal(5.5, 0.5)`; `Bag(UInteger)`→`UInteger(3, 0.0)` | `matches` handles only `isTypeOfInteger`/`isTypeOfReal`, else `null` | add 2 `matches` branches + 2 `eval` arms + 2 private helpers: **minimal +30/−1**. **REJECT** the fork's extract-method refactor of the existing Integer/Real paths (that is the whole −19). `UInteger`/`UReal` accumulators seed at (0,0), matching 7.5.0's own `isum=0`/`rsum=0.0` convention — empty-collection behaviour needs no adaptation | S7 | MEASURED |
| **K-11** | `ExpUSelect.java` (49 lines, new), `ExpUSelectC.java` (37 lines, new); `Q_USELECT_ID=12`, `Q_USELECTC_ID=13` | two new query operations, both returning `rangeExp.type()`. `uSelect`→`Set{1,2,UReal(2.0,5.0)}`; `uSelectC(…,0.9)`→`Set{1,2}`; default threshold 0.5. `uSelect` also accepts a plain `Boolean` predicate since `Boolean.conformsTo(UBoolean)`=true | absent; grammar failure `mismatched input '\|' expecting )`. 7.5.0's query ids stop at `Q_CLOSURE_ID = 11`, so **no id collision** | take both new files verbatim plus the `ParserHelper`/`ASTQueryExpression` wiring. Purely additive | S7 | MEASURED |
| **K-14** | `ExpressionVisitor.java` (49 visit methods) and its implementors | adds `visitUSelect`/`visitUSelectC` (F/:85-86); routes both to `visitQuery` | neither exists. Exactly **two** files `implements ExpressionVisitor`: `ExpressionPrintVisitor:35`, `AbstractCoverageVisitor:33` | **Tier A (must change to COMPILE, 3):** the interface, `ExpressionPrintVisitor`, `AbstractCoverageVisitor`. **Tier B (compiles unchanged, must change to be CORRECT, 1):** `EvalNode.java:351` `SubstituteVariablesExpressionVisitor` — it already overrides `visitSelect`/`visitReject`/`visitExists`/`visitForAll`/`visitOne`, so omitting the two new cases silently loses variable substitution for uSelect nodes in the evaluation browser. **Need NO edit (6).** The fork's `analysis/metrics` visitors have no port target — 7.5.0 has no such package | S7 | MEASURED |
| **G-03** ⚑ | `OCLBase.gpart` :495-500 — five fixed-arity `literal` alternatives | `UReal(v,u)`, `UInteger(v,u)`, `UBoolean(v,c)`, `UString(s,c)` (exactly 2 args) and `SBoolean(b,d,u,a)` (exactly 4). Arg-1 of `UBoolean` is `conditionalImpliesExpression`, every other argument position is `additiveExpression`, so `UReal(1 < 2, 0.1)` is rejected. Wrong arity yields bare ANTLR noise | no equivalent; a bare `UReal(2, 0.5)` reaches `ASTOperationExpression.gen` :222-224 → `Undefined operation 'UReal'.` The standard-operation map is never consulted on that path, so registering an `OpGeneric` named `UReal` does **not** help — measured even after registering `UReal` as a type name | **the ONLY fork addition that genuinely needs a grammar rule.** Keep it but drop the keyword tokens: one predicate-gated `literal` alternative delegating to a new `uncertaintyLiteral` rule, in the shape 7.5.0 already uses in this same file for query idents (`{ ParserHelper.isQueryIdent(input.LT(1)) }?` at :309/:322), plus an `input.LA(2) == LPAREN` guard to separate it from the IDENT-initial enum alternative. Move arity checking into the AST node. **NEED is MEASURED; SHAPE is INFERRED — must be validated at build time** | S6 | INFERRED |
| **G-04** | `OCLBase.gpart` :627, :631-634 (`uncertaintyType`) | adds a 4th alternative to `type`. Its action `new ASTSimpleType($name)` is *identical to what `simpleType` already does*, so its only real effect is reserving 5 more words. Side effect: `class UReal` is a parse error and **the fork cannot load its own companion library** `OCLTypes.use` (`line 9:6 no viable alternative at input 'UString'`) | 7.5.0 occupies that exact slot with `modelQualifiedType` (`IDENT HASH IDENT`), added since 2015. Built-in type names resolve through `TypeFactory.buildInTypesMap`; `Integer`/`Real`/`Boolean`/`String` are not keywords either | **delete `uncertaintyType`; keep 7.5.0's `modelQualifiedType` uncontested; register the five names in `buildInTypesMap` (T-10).** MEASURED by adding one map entry by reflection with **no grammar edit**: `let u : UReal = 1.0 in u`, `oclUndefined(UReal)`, `oclEmpty(Set(UReal))` and a model with `reading : UReal` all compile | S6 | MEASURED |
| **G-02** | `OCLBase.gpart` :337-349 (optional trailing confidence argument on `queryExpression`) | load-bearing only for `uSelectC`: `uSelectC(e \| UBoolean(e>1,0.8), 0.5)`→`Set{2,3}` but `…, 0.9)`→`Set{}`, and omitting it errors `'uSelectC' need to specify the confidence.` **Defect:** the argument is silently ACCEPTED AND DISCARDED on every other query op | never touched `queryExpression`; no collision | port the same three edits (they merge cleanly). **Under B7, fix the latent defect:** reject a confidence argument on any query ident other than `uSelectC` rather than silently discarding it | S6 | MEASURED |
| **K-09** | `ExpForAll.java:43`, `ExpExists.java:43` — defect **D-C3** | ctor passes `mkBoolean()` as the result type but evaluation returns a `UBooleanValue`, and `UBoolean.conformsTo(Boolean)` = **false**. MEASURED: `forAll`→TYPE `Boolean`, VALUE-TYPE `UBoolean` | sound: static `Boolean`, value always `BooleanValue` | **FIX (B7).** Derive the type from the predicate: `super(queryExp.type().isTypeOfBoolean() ? mkBoolean() : mkUBoolean(), …)`. The defect is LOCAL — `ExpStdOp` already computes this correctly (`true and UBoolean(true,0.5)`→static `UBoolean`); only `ExpForAll`/`ExpExists` hard-code it. Five no-regression rows pinned | S7 | MEASURED |
| **V07** | `UStringValue.java:79-91` (M-11) | `equals` unconditionally `false`: `a.equals(a)` is false; `Set{UString('a',1), UString('a',1)}->size()` = 2; `Bag{…}->count(…)` = 0 | `Value.java:183` requires OCL equality semantics; reflexivity is a Java contract | `eq = wrapper.equals(ustring.wrapper)`. Set size → **1**, Bag count → **2**, and because `equals` already lifts via `valueOf((Value) obj)` at :82, `UString('x',1.0).equals(StringValue('x'))` becomes **true** while `UString('x',0.5).equals(StringValue('x'))` stays false. **Pre-register all three flips** | S4 | MEASURED |
| **V08** | `UIntegerValue.java:57-63` (F-10) | `hash *= 7 * Double.hashCode(uncertainty())` collapses to **0 in 43 of 48** probed combinations, incl. every `u==0` case. `HashSet{1, UInteger(1,0)}.size()` = 2 because the two never share a bucket, so `equals` is never consulted | `equals`/`hashCode` consistency; `IntegerValue(1)`, `RealValue(1.0)` and `URealValue(1,0)` all hash to `1072693248` | copy `URealValue:56-63`'s zero-guarded additive body. Simulated by subclass: fixed hash `1072693248`, `equals(IntegerValue(1))` true, `HashSet{1, UInteger(1,0)}.size()` **2 → 1**. **This refutes `b7-fix-plan.md` §2 row F-10's Δoutput of `NONE`; the correct Δ is `SET`.** Corpus exposure still 0 | S4 | MEASURED |
| **V09** | `UIntegerValue.java:103-104` (M-9) + `URealValue.java:95-110` | `UInteger(1,0).compareTo(UReal(5,0))` = 0 **and** the reverse = 0 — mutually incomparable-but-equal | `Comparable` antisymmetry; `CollectionValue.getSortedElements()` :169-173 sorts with this comparator | negate the delegation in `UIntegerValue` **and** add a `UIntegerValue` arm to `URealValue.compareTo`. **M-9 alone is a no-op** (negating nothing). Port answers become **−1** and **+1** | S4 | MEASURED |
| **V10** | `UIntegerValue.java:84-86` (M-10) | `UInteger(2,0).equals(UReal(2,0))` = false and the reverse = false, because `URealValue.equals` has no `UIntegerValue` arm | no cross-type numeric equals at all | add a `UIntegerValue` arm to `URealValue.equals` lifting via `toUReal()`; both directions become **true**. **Must land together with V16** or the new equality pairs hash apart | S4 | MEASURED |
| **V12** | `SBooleanValue.java:151` (M-18) | `public int compareTo(Value o) { return 0; }` — equal to `UndefinedValue`, `StringValue`, everything. Sorting 33 of them throws nothing (a constant 0 is a *consistent* comparator) | `Collections.sort` at `CollectionValue:169-173`; Java 21 TimSort throws on *inconsistent* comparators | implement a local total order: `UndefinedValue`→+1; `SBooleanValue`→lexicographic `Double.compare` on `(belief, disbelief, uncertainty, baseRate)`; otherwise `toString().compareTo(…)`. **Do NOT delegate to `uDataTypes` `SBoolean.compareTo`** — its 0.001 L1 dead band is non-transitive and would trade the defect for a crash. *Resolves `10-values.md` §15 gap 2 as benign-today* | S4 | MEASURED |
| **V14** | `CollectionValue` :112 (`uIncludes`), :154 (`uExcludes`), :190 (`uCountC`) | with `SBooleanValue` elements: `uIncludes`→`NullPointerException: Cannot invoke "…UBooleanValue.probability()"`; `uExcludes`→`ClassCastException: A value kind of UBoolean expected`; `uCountC`→same NPE. With UReal/UString/Integer elements all three work | n/a | widen the fold to `UncertainBooleanValue` (which `uExcludes` already declares for its local) **or** document the restriction to UBoolean-kind elements. *Resolves `10-values.md` §15 gap 3, previously UNVERIFIABLE* | S4 | MEASURED |

### 2.3 Tier 3 — rows that constrain what the port must NOT do

| id | where | fork | 7.5.0 | **the PORT** | stage | conf |
|---|---|---|---|---|---|---|
| **T-01** | fork `Type.java:33`/`TypeImpl:34`/`BasicType:31` vs 7.5.0 `:31`/`:32`/`:28` | `public interface Type extends BufferedToString`, `abstract class TypeImpl implements Type`, `abstract class BasicType extends TypeImpl` | identical three declarations | **NONE.** The assumed "`Type` class vs interface" collision **does not exist**. `UncertainType extends BasicType` compiles unchanged. Any port plan budgeting for an interface/impl migration here is budgeting for nothing | S3 | READ_FROM_SOURCE |
| **T-03** | `Type.java:48,136-138`; `TypeImpl:44`; `MClassifierImpl:390` (588 lines vs the fork's 511) | no `qualifiedName()`, no `isKindOfDataType`/`isTypeOfDataType`; `MClassifierImpl` has no attribute/operation table | adds all three; the extra 77 `MClassifierImpl` lines are the attribute/operation tables pulled up from `MClassImpl`, plus `isSubClassifierOf`, `hasStateMachineWhichHandles`, `isQualifiedAccess` | **interleave, never copy a file.** File-level copying is forbidden for every file marked "edit". The five new leaf classes inherit `qualifiedName()` from `TypeImpl` and need no code | S3 | READ_FROM_SOURCE |
| **T-05** | `Type.java:63` vs the concrete types | interface declares `Set<? extends Type> allSupertypes()`; the five new types declare the **wide** form; upstream basic types narrow to `Set<Type>` | identical arrangement; `Type` is not generic in either tree | keep `Set<Type>` on edited upstream files (narrowing breaks callers) and `Set<? extends Type>` on the five new ones, exactly as the fork does. Nothing else to reconcile | S3 | READ_FROM_SOURCE |
| **T-06** | `VoidHandling` enum; `VoidType` :38,58,123,128,133 | identical enum. Five new `VoidType` overrides, each `return h == VoidHandling.INCLUDE_VOID;` | same enum; `VoidType` additionally has `isKindOfDataType` (:93-95), `allSupertypes()` throws `UnsupportedOperationException`, `conformsTo` returns true unconditionally | add the five overrides with the identical `h == INCLUDE_VOID` idiom. **Do NOT touch** `isKindOfDataType`, `conformsTo`, `allSupertypes`, `isTypeOfVoidType`, `isVoidOrElementTypeIsVoid` | S3 | MEASURED |
| **T-07** | `UniqueLeastCommonSupertypeDeterminator.java` (whole file); callers `ExpCollectionLiteral:75`, `CollectionValue:60` | byte-identical to 7.5.0 apart from a deleted `// $Id$`. Acquires uncertainty behaviour **for free** from `allSupertypes`+`conformsTo` | same class, same behaviour on classical types | **do NOT edit it** (except T-08's tie-break). Know that **this**, not pairwise `Type.getLeastCommonSupertype`, is what decides the worked example. Pairwise LCS is used by `ExpIf` :42,48 and ~50 sites in `StandardOperations*`, and the two disagree on `{UnlimitedNatural, UInteger\|UReal}` | S3 | MEASURED |
| **T-11** | `TypeImpl.java` :123-127 (size-2 fast path) / :129-146 (general loop) | with wider supertype sets the `cs.size()==2 && cs.contains(OclAny)` fast path stops firing. `LCS(Integer, Real)` intersects to `{Real, UReal, OclAny}` (general loop) vs `{Real, OclAny}` (fast path) — **same answer `Real` both ways** | fast path fires for most classical pairs | `getLeastCommonSupertype` needs **NO edit**. Do not "optimise" the general loop away and do not extend the size-2 shortcut to size 3 — the general loop is what keeps the answers right once the sets grow | S3 | MEASURED |
| **T-09** | `TupleType.java:241-264` (`genAllSuperTypes`) | cartesian product over each part's `allSupertypes()`. With `Integer` parts the size is exactly **5ⁿ+1**: n=6→15626 (130 ms), n=7→78126 (1616 ms); n=9 did not finish in 2 min | same code, base 3 (**3ⁿ+1**): n=6→730 (17 ms), n=7→2188 (14 ms). **115× faster at n=7** | no code change for correctness — `TupleType` is unedited by the fork and must stay unedited. But the port must not widen `allSupertypes` further than the fork does, and **any tuple-heavy fixture in the acceptance corpus needs an arity cap**. Hand to the harness area (H15). Not recorded anywhere in `docs/port2/` before this | S3/S9 | MEASURED |
| **T-13** | `TypeFactory.java:93-95` (`mkUReal`), `UIntegerType.java:14`, `UBooleanType:35`/`UStringType:25` vs `UIntegerType:43`/`URealType:35` | `public static Type mkUReal()` widens its return type — the only `mk*` for a basic type that does. `UIntegerType()` is `public`, siblings package-private, `URealType()` `protected`. `allSupertypes` adds `this` in two classes and `TypeFactory.mkX()` in two others (equivalent — `mkUReal()==mkUReal()` is `true`, the factory interns) | all `mk*` return their concrete class; all four basic-type constructors package-private; `allSupertypes` uses `this` uniformly | normalise the 7.5.0 way: narrow `mkUReal()` to `URealType`, make all five constructors package-private, use `this` uniformly. Source-compatible with every fork call site; **changes zero lattice cells**. Caveat: fork `TypeTest.java:392-393` does `new URealType()`, so the ported constructor must stay reachable from the same package's tests | S3 | MEASURED |
| **O-04** ⚠ | `StandardOperationsAny` :18 (registration), :130 (`Op_identical`) | registers `Op_identical` under the OCL name `equals`. `UReal(2,0.5).equals(2)`→`Boolean` (vs `UReal(2,0.5) = 2`→`UBoolean`) | **no** `equals` operation AND no `equals` token: `1.equals(2)`→`Undefined operation named 'equals'`; `1 equals 2`→`missing EOF at 'equals'` | **CORRECTED by G-01.** 03's claim that `Op_identical` is dead code without the `identicalExpression` production is **refuted by measurement**: on 7.5.0, registering `Op_identical` with **no grammar edit** makes `(1).equals(1)` work, because 7.5.0's `operationExpression` routes `.equals(…)` to the named-operation path. Register the op; delete the production | S5/S6 | MEASURED |
| **O-05** | `StandardOperationsCollection` :41-42, :261, :311 | `Set{UReal(2,0.5)}->uCount(2)`→`Integer`; `->uCountC(2, 0.9)`→`Integer` | names `uCount`/`uCountC` absent from the opmap entirely | no collision. Append at the fork's slot inside the merged `StandardOperationsCollection` (+93 lines, 0 removals, 3 registration lines) | S5/S7 | MEASURED |
| **O-08** | `StandardOperationsNumber:815` (`isInfixOrPrefix` true); `OpGeneric.stringRep` | n/a — no `pow` | `4.pow(2)` renders as `(4 pow 2)`, and `4 pow 2` does **not** re-parse (`missing EOF at 'pow'`) | **record, do not fix.** Once O-02's guard is in place no uncertain value can reach this printer, so it remains a pure upstream defect outside the uncertainty mandate | S5 | MEASURED |
| **O-09** | `OpGeneric.java`, `BooleanOperation.java` | identical to 7.5.0 apart from the six registration lines | `BooleanOperation` diffs to **0** lines; `OpGeneric` to 8, all six of which are the uncertainty registrations | **NO `OpGeneric` contract delta.** All members, both `registerOperation` overloads (the 2-arg one is needed for the fork's `toInteger` alias) and `BooleanOperation.evalWithArgs` are byte-identical. The five uncertainty registry files compile against 7.5.0's `OpGeneric` unchanged — what they will *not* compile against is 7.5.0's `Type`, which is why S5 cannot precede S3 | S5 | MEASURED |
| **O-10** | `Op_uString_uConcat.name()` returns `"+"`; the `concat` bucket | `UString('a',1).concat('b')`→`COMPILE-ERROR: Undefined operation 'UString.concat(String)'.` UString concatenation is spelled `+`: `UString('a',1) + 'b'`→`UString`, `'a' + 'b'`→`String` (unchanged) | `'a'.concat('b')`→`String` | reproduce the compile error for `UString(…).concat(…)`. **A port that "helpfully" also registers `uConcat` under `concat` invents uncertainty meaning the fork does not have** | S5 | MEASURED |
| **K-01** | `ExpSetLiteral` via the Type lattice | `Set{UReal(2,0.5), 1, 2.5}`→`Set(UReal)`; same for `Sequence(UReal)`, `Bag(UInteger)`, `Set(UBoolean)`, `Set(UString)` | compile error (control `Set{1, 2.5}`→`Set(Real)` in **both**, agreeing) | **NO collection-layer edit.** The literal's element type is the LCS, and the fork lattice (`Real.conformsTo(UReal)`=true, `Integer.conformsTo(UReal)`=true, `UReal.conformsTo(Real)`=false) yields `UReal` on its own. A consequence of the B5 lattice paid for by S3, not a work item for S7 | S7 | MEASURED |
| **K-02** | `Op_collection_max` / `Op_collection_min` | `->max()`→`UReal`/`UReal(2.5, 0.0)`; `->min()`→`UReal`/`UReal(1.0, 0.0)` | compile error; `Set{1,2.5}->max()`→`Real`/`2.5` in both | **ZERO edit** — the two classes are **byte-identical** between the trees (md5 `d28c9d9c`, `ea93b184`). `matches` returns `t.elemType()` after asking `ExpStdOp.exists("max",{UReal,UReal})`, so the `UReal` answer is discharged entirely by the scalar `StandardOperationsUReal`. Cross-area dependency, not a collection obligation | S7 | MEASURED |
| **K-07** | whole-file diff of `StandardOperationsCollection.java` | fork file is **1002 lines, LF** | 7.5.0 file is **787 lines, CRLF** (`grep -c $'\r'` → 787); a naive diff reports **+1003/−788**, i.e. −788 against a 787-line file = whole-file replacement | with `--strip-trailing-cr` the true delta is **+212/−47** across 12 hunks, and the MINIMAL edit is **+188/−18** (80 % additive). **13 of 18 operations are byte-identical**: size, count, isEmpty, notEmpty, product, flatten, asBag, asSet, asSequence, asOrderedSet, max, min, single | S7 | MEASURED |
| **K-12** | `ExpSelect:45,60`, `ExpReject:45,56` | **deliberately unchanged:** select/reject/any/one still call `assertBooleanQuery()` and REJECT a `UBoolean` predicate at compile time (`Argument expression of 'select' must have Boolean type, found 'UBoolean'.`). Only exists/forAll had the guard swapped; `uSelect`/`uSelectC` exist precisely to fill the gap | identical — the whole `ExpSelect` diff is +2/−0 and both lines are `$Id$`/`@version` comments | **PRESERVE THE ASYMMETRY.** It is uncertainty design, not an accident. Do not "helpfully" relax select/reject/any/one to accept `UBoolean` | S7 | MEASURED |
| **K-13** | `ExpQuery` `sortedBy`, hunk `@@ -456,12 +655,7 @@` | only `return new SequenceValue(rangeElemType, result);` — the fork predates the feature | has an extra `if (this.type().isTypeOfOrderedSet()) return new OrderedSetValue(…)` branch | **REJECT the hunk.** Upstream drift, not uncertainty meaning, so clause 2 gives it to 7.5.0. Applying the fork here would silently **REGRESS** 7.5.0. The clearest case in the whole adaptation of the policy's second clause, and why whole-file replacement is unacceptable | S7 | READ_FROM_SOURCE |
| **K-17** | `CollectionValue.uIncludesAll` / `uExcludesAll` — defects **D-C5**, **D-C6** | `uIncludesAll` short-circuits `if (coll2.size() > size()) result = FALSE;` **without examining any element**, while `uExcludesAll` has no matching shortcut; both `and`-fold per-element probabilities, silently treating element memberships as **independent** events | plain boolean set operations, no size shortcut | **D-C5 FIX:** drop the shortcut or guard it to non-Bag receivers — size is not a membership criterion for a Bag, and the asymmetry with `uExcludesAll` is unjustified. **D-C6 DOCUMENT:** the independence assumption is a modelling choice, not a bug, but it silently determines every `includesAll` probability and is written down nowhere in the fork | S7 | READ_FROM_SOURCE |
| **P-03** | `UBooleanExpression.in` :8,11,14 and `URealExpression.in` :62,65; thrown at `ASTURealLiteral` :28,31 and `ASTUBooleanLiteral` :31 | bare messages with **no** `src:line:col` prefix, because they use `new SemanticException(String)` with a null `SrcPos`. Raw buffer: `>>>Value must be Boolean\n<<<`. Ordinary fork errors DO carry the prefix. **Corrected count** (`adaptation-policy-refutation.md` §4.1): not four in one place — **eleven position-less strings across five classes in two packages** (`ASTURealLiteral`, `ExpConstUInteger`, `ExpConstUBoolean`, `ExpConstUString`, `ExpConstSBoolean`); the `UBoolean` message actually lives in `ExpConstUBoolean` (`uml/ocl/expr`), not `ASTUBooleanLiteral` (`parser/ocl`) | **NOT a collision.** `SemanticException` is byte-identical in both trees and 7.5.0 has **16** position-less sites of its own, one in the OCL expression parser: `Sequence{1..'a'}`→bare `Ranges must be of type Integer.` | **no normalisation rule.** Three binding constraints: **C1a** keep all eleven strings byte-exact, including the ungrammatical `Probability must be a Integer or Real` and the `SBoolean` messages' double spaces (`Belief  must`); **C1b** throw with **NO** `SrcPos`/`Token` (attaching one prefixes `probe:1:0: ` and breaks all 5 corpus error-path entries); **C1c** keep them the only thing written to the error stream (the harness captures a buffer, not a line — M-49b). Only 4 of the 11 have a corpus witness; the other 7 are unwitnessed and the guard must still cover them | S8 | MEASURED |
| **P-05** | `Value.java`, `SetValue`, `CollectionValue.getSortedElements`, `TupleValue`, `VoidType.toString`, all scalar `toString`s | **identical code** to 7.5.0 — diffs are only `$Id`/`@version`/javadoc lines plus the fork's added `RealValue.valueOf` (E26) | same | **NO collision, so no rule.** Confirmed by a 21-expression crisp control: 16 lines identical, 5 differing only by the `Undefined` token. Rules that would normalise separators, whitespace, sort order or the `" : Type"` suffix are **explicitly refused as divergence-masking** | S8 | MEASURED |
| **P-06** | collection printing sorts via `Collections.sort`; several `compareTo` bodies fall back to `toString().compareTo(…)` | over the 324 distinct element strings the fork actually printed, exactly **one** flips sign under the rename: `'false'` (`'f'`=102 lies between `'U'`=85 and `'n'`=110) | same machinery, same fallback | **PROVEN UNREACHABLE**, not merely unwitnessed: every `compareTo` in the port's universe short-circuits before the fallback. The one sign-flipping string is produced by `BooleanValue`, which is in the short-circuiting group. 12 probes agree; corpus exposure 0. **This is why N1 is a whole-string map and not `expected.replace("Undefined","null")`.** After DEP-07 lands the argument holds *a fortiori* — the three classes that today fall out of the if-chain returning 0 will return +1 explicitly | S8 | MEASURED |
| **P-07** | `UIntegerExpression.in` :44,45,46 and `URealExpression.in` :179-185 | `UInteger(5, 0.3).toString()`→`'UInteger(5, 0.3)' : String` — the U-value printed grammar quoted **inside** a `StringValue` | no counterpart | **no normalisation.** Constraint **C2**: the UReal/UInteger printed grammar is pinned **twice** — directly by 696 entries (486 UReal + 210 UInteger) and indirectly by these 10. Any change to those `toString` bodies must be justified against both populations; a "printing-only" change is not local | S8 | MEASURED |
| **P-09** | `UStringValue` :67-72, `SBooleanValue` :124-130 | `UStringValue.toString` HARD-CODES `"UString('"` instead of `type()` and leaves the confidence unrounded; `SBooleanValue.toString` uses `MathUtil.round(x,3)` four times, **not** the vendored library's `String.format("SBoolean(%5.3f, …)")` | no counterpart | port both bodies **verbatim**. `grep -c 'UString\|SBoolean' *.in` is **0** in all four corpus files, so under B2 their printed grammar is asserted by **nothing** and needs purpose-built printing tests. Two traps: routing either through the library emits space-padded fields, and `String.format` is locale-sensitive whereas `StringBuilder.append(double)` is not | S8 | READ_FROM_SOURCE |
| **V04** | `uDataTypes.*.toString()` | delegates format with `%5.3f` — `javap` shows `String UReal(%5.3f, %5.3f)`; executed, `new UReal(2,0.5).toString()` gives `UReal(2.000, 0.500)` | n/a | confirmed **UNREACHABLE** from the user: every `*Value.toString(StringBuilder)` composes the text from its own accessors, and the only cross-package delegate escape (`StandardOperationsCollection:630`) is arithmetic. **The port must keep it unreachable.** Trap: `UBooleanValue`'s 3-dp rounding and the delegate's `%5.3f` coincide numerically but differ textually (`0.5` vs `0.500`) | S4 | MEASURED |
| **V05** | `UStringValue.java:67` | hard-codes the literal `"UString('"` rather than calling `type()`, unlike all four siblings | n/a | keep as-is; the printed form is pinned by V03. Record the divergence so it is not "tidied" later | S4 | READ_FROM_SOURCE |
| **V06** | `URealValue:95`, `UIntegerValue:94`, `SBooleanValue:151` | `compareTo(UndefinedValue)` returns **0** for all three (`UBooleanValue` and `UStringValue` correctly return +1) | `IntegerValue(1).compareTo(Undefined)`→1 and the reverse→−1; `Value.java:27-31` states *"all values must be able of being compared with an UndefinedValue"* | all five uncertain value classes must return **+1** against `UndefinedValue`. B7 tier-3 row 10; an intended departure from three fork answers | S4 | MEASURED |
| **V11** | `UBooleanValue.java:233-234` (M-8) | `UBooleanValue.FALSE.equals(BooleanValue.FALSE)` = false — the `&& !this.value()` conjunct is **dead**, because `valueOf` normalises every instance to `value == true` | `BooleanValue.equals` rejects all non-`BooleanValue`, so the reverse direction stays false regardless | delete `&& !this.value()`; the answer becomes **true**. The asymmetry with `BooleanValue.equals` remains a **declared residual** (`BooleanValue` is upstream and untouched) | S4 | MEASURED |
| **V13** | `UStringValue.java:95-104` (M-12) | `UString('abc',1).compareTo(StringValue('abc'))` = **12**, i.e. `'a'`(97)−`'U'`(85): it compares the raw string against the *rendered* form. The reverse, −46, is upstream `StringValue` falling back to `toString()` | `StringValue.compareTo` compares characters | fix **only** the `StringValue` arm at :103 → `wrapper.getString().compareTo(((StringValue) o).value())`, answer becomes **0**. Leave the UString-vs-UString `toString()` route at :100 alone; it is self-consistent and total, and changing it is a separate decision | S4 | MEASURED |
| **V16** | `URealValue.java:56-64` vs :67-91 (F-3) | `hashCode` hashes **unrounded** `value()`/`uncertainty()` while `equals` compares them **rounded to 10 dp**, so two equal values can land in different buckets | `Value.java:175/:183` carry the standard consistency obligation | round inside `hashCode` with the same `MathUtil.round(x, 10)` the `equals` arm uses, in the same order. **Must land with V10** | S4 | READ_FROM_SOURCE |
| **K-05** | `StandardOperationsCollection` registration block + 2 new classes | `Op_collection_uCount` (default threshold 0.5) and `Op_collection_uCountC` (explicit threshold, `RuntimeException` outside [0,1]), both returning `Integer` | absent: `Set{1,2,3}->uCount(1)`→`Undefined operation named 'uCount'` | purely additive: +93 lines, 0 removals, plus 3 registration lines. No existing operation disturbed | S7 | MEASURED |
| **K-15** | `ExpressionPrintVisitor.java:455-456` — defect **D-C7** | `visitUSelectC` routes to the generic `visitQuery`, which knows nothing of `fUncertaintyExp`. `uSelectC(e \| e >= 1, 0.9)` prints as `…->uSelectC(e : UReal \| (e >= 1))` — the threshold is dropped, so `…, 0.9)` and `…, 0.1)` print identically despite yielding `Set{1,2}` vs `Set{1,2,UReal(2.0,5.0)}` | n/a | **FIX (B7):** give `visitUSelectC` a dedicated body that appends `getUncertaintyExpression()`. Round-tripping a printed expression must not lose the threshold | S7 | MEASURED |
| **K-16** | `ExpQuery` :360-364, callers :382,388 — defect **D-C8** | retains `evalExistsOrForAll0` marked `@deprecated` after replacing it; its only remaining callers are its own recursive calls, i.e. dead code | `ExpQuery` :165-221 (57 lines) is the live implementation | **FIX (B7): DELETE** 7.5.0's copy rather than carry a dead duplicate. This raises the minimal `ExpQuery` edit to **+158/−60** (vs the fork's own +163/−9) and is the difference between porting and copying | S7 | READ_FROM_SOURCE |
| **K-18** | `ExpQuery` :224-240, throw at :236 — defect **D-C9** | out-of-range confidence throws a bare `RuntimeException` at **EVAL** time: `uSelectC(e \| e >= 1, 1.5)` compiles cleanly, then `java.lang.RuntimeException: Confident value must be between 0 and 1, found '1.5'` | reports argument-domain violations as `ExpInvalidException` at **compile** time | **FIX (B7), express it the 7.5.0 way:** reject a **constant** out-of-range threshold at compile time via `ExpInvalidException`, retaining the runtime check only for computed thresholds | S7 | MEASURED |
| **G-05** | `spec-parts/13-grammar.md` §13.0 vs the actual files | fork `.gpart` files are **LF** (`tr -cd CR \| wc -c` = 0; first bytes `0a 2f 2a 0a`) | 7.5.0 `.gpart` files are **CRLF** (677 CR bytes in a 677-line file; `file(1)` reports "with CRLF line terminators"). Two exceptions are LF on both sides — `USEBase.gpart`, `Generator.gpart` | **correct §13.0: it states the direction backwards.** Its conclusion (normalise before diffing) stands. Trap, quantified: raw diff calls `OCLLexerRules` 254/254 lines changed when the real delta is **0**; `ocl/OCL` 130/130 when it is **0**; `SoilBase` 1062 when it is **2**. **Every diff in this programme must use `tr -d '\r'` or `--strip-trailing-cr`** | S6 | MEASURED |
| **P-08** | `b7-fix-plan.md` §3.1 rows M-49b, M-31, M-32, F-4, M-29, M-28, M-37, M-38, M-10 | 33 behaviour-changing rows; the plan classifies 24 as corpus-impact-zero | n/a | independently re-checked **by running the corpus** rather than reading the ledger: M-49b changes the *selector*, not the 5 expected strings (0); M-31/M-32's 2 entries are at risk only if the check moves, and C1b makes not-moving binding (0); F-4's 3 entries are widening-safe (0); M-29's 2 candidates never reach eval (0); M-28's 3 candidates are inside N1's 79 (0); M-37's 9 entries already print `getRuntimeType` (0); M-38's `UBooleanExpression.in:143` evaluates without NPE (0). **B7-driven never-pass count = 0 certain, 1 UNVERIFIABLE candidate** (`UIntegerExpression.in:1304`) | S8 | MEASURED |
| **P-02** | the 79 entries, by file: UBoolean 16, UInteger 38, UReal 25, UCollection 0 | all 79 are the **WHOLE** expected string; `grep -h '^->.*Undefined' \| grep -vc '^-> Undefined : OclVoid$'` = **0**. The fork's own actual output over all 1427 entries nests the token **0** times | prints `null : OclVoid` for all 79 | rule **N1**. Narrowness rests on 4 checkable facts: anchored to the complete string; the `: OclVoid` suffix must also match so a right-text/wrong-type port still fails; the justification is a one-line source diff; cardinality pinned at 79 from three independent greps. **Three mutation controls must turn the suite red** (revert the token in the port; make one Undefined-returning op return a value; substitute a substring replace for the whole-string map) | S8 | MEASURED |

### 2.4 Rows needing action before their stage starts

| id | why | what to do | owner |
|---|---|---|---|
| **G-03** | the predicate-gated `literal` rule is the only **INFERRED** shape in the whole adaptation. The parser was not regenerated (needs Maven + the ANTLR plugin) | build it and validate the ANTLR 3 lookahead interaction between the new IDENT-initial `literal` alternative, the existing `enumName=IDENT '::' enumLit=IDENT` alternative, 7.5.0's `modelQualifier=IDENT HASH …` enum alternative, and `propertyCall` | S6, first task |
| **T-08 / B11b** | new open decision; no single well-defined "reproduce the fork" answer exists | user decides fix-with-deterministic-tie-break (recommended) vs inherit-the-coin-flip | user, before S3 |
| **V15 / K-10** | **the two readers disagree** on whether B7 authorises a fix | user decides; also fixes the value of `Set{1, UReal(1,0)}->size()` | user, before or with S4's F-10 commit |
| **V18 / P-04 (R1)** | 8 corpus rows cannot pass against the fork's own jar; S8's target is 1419 or 1427 and the two are not interchangeable | user decides (a) fork code wins, 3 dp, 8 pre-registered; or (b) corpus wins, 10 dp | user, before S8 |
| **O-04 vs G-01** | **the two readers disagreed and it is now resolved by measurement** — 03 said `Op_identical` is dead without the grammar production; 04 measured it working on 7.5.0 with no grammar edit | no action beyond following G-01; recorded so the resolved claim is not re-litigated from 03 | S5/S6 |
| **K-gap-2 vs O-07** | **resolved by cross-reading** — 06 recorded `Op_boolean_or` vs `Op_uBoolean_or` dispatch for `(Boolean, Boolean)` as "UNVERIFIABLE as specified behaviour, opmap iteration order". 03 establishes that `ExpStdOp.opmap` is an insertion-ordered `ArrayListMultimap` with first-match-wins, and the 74 970-cell sweep at the fork's slot is the deterministic baseline | no new mechanism. **The determinism is bought by O-07's registration slot** — keep it, and 06's five no-regression rows are the acceptance criterion | S5, verified in S7 |

---

## 3. Waiver ledger

**One waiver, W-01** (`TypeTest#testSupertype`, 10 of 12 assertions falsified by adopting the fork's
`allSupertypes()` widening — B5 option 1), now written up canonically, with its full evidence and the
corrected "why the alteration is correct" text, in `upstream-test-waivers.md` (§§2-3 there). That file
is normative for W-01; this section is not duplicated here.

Two standing notes that belong to this document rather than to the waiver record itself:

1. **Coverage, not breakage.** `TypeTest`'s ~39 `testIsTypeOf*`/`testIsKindOf*` methods each want ten
   more `assertFalse` lines once `Type` gains ten predicates. Omitting them **fails nothing**, so it is
   not covered by W-01 — budget for it, do not waive it.
2. **T-08 / B11b is a nondeterminism, present in plain 7.5.0**, not a waiver. It breaks no upstream
   test today only because no upstream fixture puts `*` inside a collection literal — that is luck,
   not safety, and it is not covered by W-01 or by B11.

**Rule-3 obligation (discharged):** S3 wrote W-01 into `upstream-test-waivers.md`. Target was, and
remains, **exactly one waiver**.

---

## 4. Intended departures — pre-registered

Every place the port deliberately differs from **the fork**. These exist so S4–S8 can tell an
intended correction from a porting error. **Ids are stable; cite them in stage reports and in the
differential harness's sign-off mechanisms.**

Legend for **origin**: `B7` = a fixed ledger defect · `750` = 7.5.0's convention wins ·
`NEW` = a defect found in this adaptation round, not previously on any list ·
`PEND` = blocked on a user decision in §5.4.

| id | origin | departure from the fork | observable change | row |
|---|---|---|---|---|
| **DEP-01** | 750 / B6 | `UndefinedValue` prints `null` | every `Undefined` becomes `null`, at top level and nested; 79 corpus rows normalised by N1 | V01/P-01 |
| **DEP-02** | B7 (M-11) | `UStringValue.equals` implements real equality | `a.equals(a)` false→**true**; `Set{UString('a',1),UString('a',1)}->size()` 2→**1**; `Bag->count` 0→**2**; `UString('x',1.0).equals(StringValue('x'))` false→**true** (0.5 stays false) | V07 |
| **DEP-03** | B7 (F-10) | `UIntegerValue.hashCode` zero-guarded additive body | hash 0→`1072693248`; `HashSet{1, UInteger(1,0)}.size()` **2→1** — the UInteger is silently dropped. **`b7-fix-plan.md`'s Δoutput `NONE` for F-10 is wrong; it is `SET`** | V08 |
| **DEP-04** | B7 (F-3) | `URealValue.hashCode` rounds with the same `MathUtil.round(x,10)` `equals` uses | equal values stop landing in different buckets. Must land with DEP-05 | V16 |
| **DEP-05** | B7 (M-10) | `URealValue.equals` gains a `UIntegerValue` arm lifting via `toUReal()` | `UInteger(2,0).equals(UReal(2,0))` and the reverse both false→**true** | V10 |
| **DEP-06** | B7 (M-9) | `UIntegerValue.compareTo` delegation negated **and** `URealValue.compareTo` gains a `UIntegerValue` arm | `UInteger(1,0).compareTo(UReal(5,0))` 0→**−1**; reverse 0→**+1**. *M-9 alone is a no-op* | V09 |
| **DEP-07** | B7 | `URealValue`/`UIntegerValue`/`SBooleanValue`.`compareTo(UndefinedValue)` returns **+1** | 0→+1 in three classes; strengthens the P-06 unreachability proof | V06 |
| **DEP-08** | B7 (M-8) | `UBooleanValue.equals` drops the dead `&& !this.value()` | `UBooleanValue.FALSE.equals(BooleanValue.FALSE)` false→**true**. The reverse direction stays false — **declared residual** | V11 |
| **DEP-09** | B7 (M-18) | `SBooleanValue.compareTo` becomes a local total order | constant 0 → `Undefined`:+1, lexicographic `(b,d,u,a)`, else `toString()`. **Not** delegated to `SBoolean.compareTo` (0.001 dead band is non-transitive) | V12 |
| **DEP-10** | B7 (M-12) | `UStringValue.compareTo`'s `StringValue` arm compares raw strings | `UString('abc',1).compareTo(StringValue('abc'))` 12→**0**. UString-vs-UString route unchanged | V13 |
| **DEP-11** | NEW | `CollectionValue` u-ops accept `UncertainBooleanValue` elements (or the restriction is documented) | `Set(SBoolean)` `uIncludes`/`uExcludes`/`uCountC` stop throwing NPE/CCE | V14 |
| **DEP-12** | B7 (F-2) | `MathUtil.round` saturation above 9.2e8 fixed — **in a second commit**, after the verbatim body has shown the 10-decimal assertions green | large-magnitude roundings stop saturating | 02 §5.4 |
| **DEP-13** | PEND (R1) | `UBoolean` confidence printed at **3 dp** (fork *code*), so 8 corpus rows recorded at 4/9/10 dp become **pre-registered non-agreements**, each pinned to its measured fork-jar string | S8 target becomes **1419/1427**, not 1427/1427 | V18/P-04 |
| **DEP-14** | 750 | `Op_number_sqrt` retained and guarded | `4.sqrt()` answers 7.5.0's `Integer`/`2.0 : Real` where the **fork errors**; `UReal(4,2).sqrt()` still `UReal` | O-01 |
| **DEP-15** | 750 | `Op_number_pow` retained and guarded on both parameters | `4.pow(2)`→`Real` where the fork has no `pow`; `UReal(4,2).pow(2)` still a clean compile error; `power` still `UReal`. **Not** aliased | O-02 |
| **DEP-16** | B7 | `Op_uString_uConcat` registered **once**, length guard hoisted | `+ 'a'`, `+ true`, `+ Set{1}`, `+ UBoolean(…)`, `+ SBoolean(…)` stop throwing `ArrayIndexOutOfBoundsException` and produce 7.5.0's clean `Undefined operation` diagnostics | O-06 |
| **DEP-17** | 750 / B4 | `identicalExpression` deleted; `equals` is an ordinary `IDENT`; `Op_identical` registered in the opmap | `X->equals(Y)` now parses (fork: error); `X.equals(Y) and Z` now parses (**fork defect D1**); `A and X.equals(Y)` binds correctly (**fork defect D2**, fork mis-parses as `(A and X).equals(Y)`); user-defined `equals` dispatches (impossible in the fork); `equals` usable as let/tuple/iterator/operation name; **6 reserved words × 6 lexers = 36 reservation sites → 0** | G-01 |
| **DEP-18** | 750 | the five U-type names are resolved by `TypeFactory.buildInTypesMap`, not reserved by the grammar | `class UReal` compiles (fork: parse error); the fork's own companion library `OCLTypes.use` becomes loadable, removing a self-inconsistency. **Accepted consequence:** the five names still shadow a user classifier of the same name, because `ASTSimpleType.gen` consults the map first | G-04/T-10 |
| **DEP-19** ⚑ | 750 | U-literal arguments admit full `expression` via the predicate-gated rule; arity checking moves into the AST node | strictly **wider** than the fork (`UReal(1 < 2, 0.1)` becomes legal where the fork rejects it); arity errors become real diagnostics instead of ANTLR noise. **Deliberate, documentable widening; no corpus entry relies on the rejections** | G-03 |
| **DEP-20** | B7 | a confidence argument is **rejected** on any query ident other than `uSelectC` | `Set{1,2,3}->select(e \| e > 1, 0.9)` stops being silently accepted-and-discarded | G-02 |
| **DEP-21** | B7 (D-C3) | `ExpForAll`/`ExpExists` derive the static type from the predicate | `Set{1,2,UReal(2,5)}->forAll(…)` static type `Boolean`→**`UBoolean`**; values unchanged; `value.type().conformsTo(expression.type())` holds. Five pure-Boolean no-regression rows must stay `Boolean` | K-09 |
| **DEP-22** | PEND (V15) | set-literal membership becomes source-order-independent | `Set{1,UReal(1,0)}->size()` and `Set{UReal(1,0),1}->size()` must agree with each other **and** with `Set{UReal(1,0)} = Set{1}`. **This document does not assert whether the answer is 1 or 2** | V15/K-10 |
| **DEP-23** | B7 (D-C5) | `uIncludesAll`'s unguarded `size()` shortcut dropped or restricted to non-Bag receivers | `uIncludesAll` stops returning FALSE without examining any element; symmetry with `uExcludesAll` restored | K-17 |
| **DEP-24** | B7 (D-C7) | `visitUSelectC` prints its confidence argument | `uSelectC(…, 0.9)` and `uSelectC(…, 0.1)` stop printing identically | K-15 |
| **DEP-25** | B7 (D-C8) | 7.5.0's `evalExistsOrForAll0` (57 lines) is **deleted**, not carried dead | minimal `ExpQuery` edit +158/−60 instead of the fork's +163/−9 | K-16 |
| **DEP-26** | B7 (D-C9) | a **constant** out-of-range `uSelectC` threshold is rejected at compile time via `ExpInvalidException` | `uSelectC(…, 1.5)` fails to compile instead of throwing a bare `RuntimeException` at eval. Runtime check retained for computed thresholds | K-18 |
| **DEP-27** ⚑ | 750 | `exists`/`forAll` short-circuit restored on the crisp path — break out of the fold when the accumulator is still a plain `BooleanValue` at the absorbing element | recovers 7.5.0's cost and semantics for every expression with no uncertainty in it, confining B9's regression to genuinely uncertain predicates. **INFERRED — no implementation exists in either tree; must be validated against `ExpQueryTest` and the five no-regression rows** | K-08 |
| **DEP-28** | 750 | 7.5.0's `sortedBy` `OrderedSet` branch retained; the fork's older body **rejected** | `->sortedBy` over an `OrderedSet` keeps returning an `OrderedSetValue`. Applying the fork here would silently regress 7.5.0 | K-13 |
| **DEP-29** | 750 | `mkUReal()` narrowed to `URealType`; all five constructors package-private; `allSupertypes` uses `this` uniformly | **zero** lattice cells change; source-compatible with every fork call site. Constraint: the constructor must stay reachable from the same package's tests | T-13 |
| **DEP-30** | PEND (B11b) | `UniqueLeastCommonSupertypeDeterminator.calculateFor` gets a deterministic tie-break | `Set{*, 1}` stops being a coin flip. **Not covered by B11's waiver and bites even with B11 reproduced verbatim** | T-08 |
| **DEP-31** | 750 | 7.5.0's import style retained: `import java.util.*` in `CollectionValue`, explicit imports in `StandardOperationsCollection` | none behaviourally; prevents the fork's restyle from inflating the diff | K-06/K-03 |
| **DEP-32** | 750 | the fork's extract-method refactor of `Op_collection_sum`'s Integer/Real paths **rejected** | `sum` edit is +30/−1 instead of +57/−19 | K-03 |
| **DEP-33** | 750 | the fork's `analysis/metrics/{AbstractMetricVisitor,GSMetricVisitor}` are dropped | 7.5.0 has no `analysis/metrics` package; they have no port target | K-14 |
| **DEP-34** | 750 | the upstream rename `visitObjOp`→`visitInstanceOp` is **not** reverted | the fork's `ExpressionVisitor` diff is +10/−1; the −1 is upstream drift and stays | K-14 |

**Standing rule.** A behavioural difference from the fork that is **not** in this table is a porting
error until proven otherwise. Adding a row requires the same shape as the rows above: origin, the
observable change, and the collision row it comes from.

---

## 5. Re-scoring the open decisions — outcome

`foundation-verdict.md` §3 listed **B1–B12** and **H13–H22** (23 rows); 8 were already answered before
this round. Of the 15 that remained, this document **answered 5** (B4, B5, B6, B8, B9 — each decided
by measurement, not preference; the answers are folded into §§1-4 above and need no separate record
now that the port has shipped), **constrained 6** without deciding them (B1, B10, B11, B12, H15, H16),
**left 4 fully open** (B1a, H13, H18, H22 — none of them adaptation questions), and **raised 3 new
ones** (B11b, B13, B14).

**Where the constrained and new decisions landed**, now that S3–S9 are done: B1 → V04's
unreachability constraint holds, `uDataTypes` stayed vendor-relocated. B10 → dropped, as recommended
(dead code, unreachable from any grammar). B11 → reproduced, with **B11b split off and FIXED**
(deterministic tie-break, DEP-30) because the original nondeterminism existed in plain 7.5.0 too and
made the oracle unmeasurable. B12 → the corpus criterion **1427/1419/8-departures/0-`.in`-edits**
shipped as stated (see the correction to "four" below). H15 → `UString`/`SBoolean` corpus gaps closed
by purpose-built tests, not by widening the historical `.in` fixtures. H16 → `equals(Object)`
fidelity is evidenced by the differential harness's purpose-built tests, per `b7-fix-plan.md` §7.3.
**B13** (= V15/K-10) and **B14** (= V18/P-04) were both genuine scope questions this document
deliberately left to the user; see `upstream-test-waivers.md` and `b7-fix-plan.md` for where each was
ultimately decided — this document does not re-litigate them.

### 5.1 Corrections this policy made to the standing record

Five, each measured; S3–S9 used the corrected form. Kept because two are still live pointers into
`b7-fix-plan.md`: (1) `b7-fix-plan.md` §2 row F-10's Δoutput `NONE` was **wrong** — the fix changes
`HashSet` membership (`SET`, not `NONE`) because the unfixed hashes never collide with the fixed ones;
zero corpus exposure survives on a different argument. (2) `b7-fix-plan.md`'s S8 criterion "1427
entries, 1427 passes, zero expectation edits" was **unachievable** — the fork fails 8 of its own 1427
entries on its own shipped jar; restated as **1427 entries / 1419 passes / 8 pre-registered departures
/ 0 `.in` edits**. (3) `spec-parts/13-grammar.md` §13.0 had the fork/7.5.0 `.gpart` line-ending
attribution **inverted** (fork is LF, 7.5.0 is CRLF; the normalise-before-diffing conclusion still
stands). (4) `spec-parts/15-upstream-delta.md` claimed registry slot order changes `Integer + Integer`
overload resolution — **measured false**; the real constraint is branch order inside
`ArithOperation.matches` (O-07), a different constraint from the same area. (5) `adaptation/03-operations.md`
C-04's claim that `Op_identical` is dead code without the `identicalExpression` grammar production was
**refuted by measurement** — registering it needs zero grammar edit (this is what makes B4 answerable).

---

## 6. Residual risk — what remained open, where it landed

This section tracked, pre-implementation, everything not yet established: 2 INFERRED shapes needing a
build-time probe (G-03's predicate-gated grammar rule; DEP-27's exists/forAll short-circuit), 5 claims
unverifiable until the port existed (an unguarded `sqrt` `ClassCastException`; whether the V09/V13
`compareTo` fixes move any corpus print order; M-10's `UIntegerExpression.in:1304` watch item;
`ShellIT`'s `t133` import chain; cell-for-cell lattice agreement against the fork jars), 7 measured-but-
bounded gaps in the sweeps (chiefly: **`UString`/`SBoolean` have zero corpus coverage** — the 4-arg
`SBoolean` literal and B2's 39 operations are evidenced only by purpose-built tests, never by the
historical `.in` corpus; the operation sweep covered only arities 1-3 over 17 types; the corpus itself
was never replayed through the operation registries, only through the 179-expression and 74970-cell
sweeps), and 5 environment/process risks (no Maven run during this round; `ShellIT` not executed;
the fork probed only on OpenJDK 21, not its contemporary JDK 7/8; jar-vs-source provenance asserted
not proven).

The port shipped and both acceptance commands are green (`harness-contract.md` §0.1), so these are
now closed as evidence gaps rather than open risks — S3–S9 either produced the purpose-built tests
this section called for or the differential sweep exercises the cells directly. The three standing
**implementer traps** remain live as guidance for anyone touching this code later, since a correct
build does not by itself prevent someone from silently reintroducing the failure mode:

| id | the trap | the guard |
|---|---|---|
| **R-A** | an implementer "improves" the eleven position-less `SemanticException(String)` throws (P-03) into positioned ones — all 5 corpus error-path entries then fail with a *plausible-looking* `probe:1:0: ` prefix that reads like a real divergence | pin all eleven strings as constants (not four — P-03's corrected count), plus one test asserting the captured buffer carries **no** `probe:` prefix (C1a/C1b/C1c) |
| **R-B** | a reviewer diffs `IntegerType.conformsTo`, sees a body byte-identical to 7.5.0's, and "restores symmetry" — silently widening the lattice, because `Integer ≤ UInteger`/`UReal` is installed from the **supertype** side | javadoc note on `IntegerType.conformsTo` saying it is deliberately unchanged (T-04) |
| **R-C** | the P-06 unreachability proof enumerates exactly **ten** `compareTo` implementations; it must be re-run if a sixth uncertain value class is added, or any class lacking an explicit `UndefinedValue` branch **and** having a `toString` fallback is added | N1-G is the standing guard: any expectation containing `Undefined` outside the whole-string form makes the run **fail** rather than guess |

---

## 7. Checklist for a stage author

1. Read §1. Apply the four questions in order. If the answer is not obvious, the case is in §2.
2. Find your rows in §2 by stage. Do every `⚑` row's probe **before** writing code.
3. Every behavioural difference from the fork that you produce must already be in §4. If it is not,
   either it is a porting error or §4 needs a new row — decide explicitly, do not proceed.
4. If an upstream test fails, the default conclusion is that your change is wrong. The **only**
   sanctioned waiver is **W-01** (§3). Adding a second requires the same evidence shape as §3.2.
5. Never copy a fork file over a 7.5.0 file. Never diff without `tr -d '\r'`.
6. Paste `git diff --name-status` over `*/src/test/*` in your stage report (rule 3 has no automatic
   signal until B3's profile runs).
